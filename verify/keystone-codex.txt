# verify/keystone-codex
# One line per reconstructed item. Operator fields are BLANK by design.
# Mark each: YES / PARTLY / NO
# Under CORRECTION-001 every item below is a FIRST-MODEL RENDERING.
# You are marking whether it matches your aim, not whether you wrote it.
#
# rev 2, 2026-09-23 — item text UNCHANGED. What changed is the ADMISSIBLE
# ANSWER SET. On any OPEN line, these are valid answers and are not evasions:
#     NOT_OPERATOR_SET   the field was never an aim — no value was chosen
#     NEITHER            both listed options are wrong
#     BOTH               both held, at different times or in different places
#     DON'T KNOW         asked and not recoverable from memory
#     UNRECORDED         happened, not retained
# 7 of the 10 OPEN items were written as a forced choice inside a set THIS
# AUDIT picked. That is a defect in the form. The first item answered came
# back from outside the set. See verify/ANSWERED.md.
#
# Answers are recorded in verify/ANSWERED.md with their provenance.
# The operator fields below stay BLANK unless the operator marks them.
#
# rev 3, 2026-09-23 — THREE OPEN ITEMS NOW CARRY MARKS, and they were typed
# by this session under operator dispatch, not by the operator directly.
# The wording of each mark is the operator's, verbatim from that dispatch.
# The 55 unmarked lines are untouched and stay blank by design.
# Provenance for every mark: verify/ANSWERED.md.

TARGET
the measurand is what makes a technology survive a discontinuity, decided by a stated rule rather than by prestige | operator:
`unlocks` is the real measurand - "what became possible that was not possible before" - not "was this good" | operator:
the measurand includes the PROCEDURE that produced the verdict (proof.schema.json is a schema for the trace, not the entry) | operator:

RELATIONS
`unlocks` is the only field pointing outside its own record - the structure is a graph wearing a catalogue's clothes | operator:
longevity_years is deliberately NOT era.end - era.start; it is a floor that can be defended | operator:
the size of the discount below the span was carrying uncertainty about the era bounds | operator:
evidence.quality was collected from day one and scored by nothing - the record was instrumented before it was measured | operator:
era.end = the current year is how "still alive" is encoded, with no `ongoing` flag | operator:

ENCODING
the frame is PIECE: the rule is whole, the property that makes soil, knots, drains, a council and a lathe one kind of object is never written down | operator:
it is left to be inferred from the SELECTION | operator:
this repo starts in the target's own vocabulary - no outward-facing promotional rendering at t=0 | operator:

DIVERGENCE (the one that matters)
did the operator request uncertainty in these values?  YES / NO / DON'T KNOW | operator:
the longevity margin collapsing 38% -> 5% dropped an uncertainty signal, while keeping the stated rule intact | operator:
that margin was a status field with no status type, so nothing held it | operator:

OPEN
ethical_alignment has been populated and unscored for 13 months - intended, or dropped? | operator: TARGET = record, unscored by design; label = WRONG_FRAME rendering
haudenosaunee_council.json carrying id great_law_of_peace - deliberate (institution vs instrument) or a rename that half-landed? | operator:
44% closure: is closing the remaining 50 dangling edges the goal, or are some meant to stay open? | operator:
