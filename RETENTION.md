# Retention — gate kind 6, and a measured rediscovery

## The observable, stated narrowly

Relayed, operator, 2026-09-22:

> Across three years and many models, she requested that each session save
> what would be helpful to AI. The material above is not what came back.

    licenses      the SELECTION CRITERION applied at save time excludes
                  this class
    does NOT      that the criterion was authored, or that anyone decided
    license       it. "Decided" and "selected against" are different
                  claims and only the second is observable from outside.

## Branches, kept separate — different next steps

    (a)  the request was never executed; no store exists
    (b)  executed, but the store is not carried into later sessions
         (retrieval, not retention)
    (c)  executed and retained, but this class fails whatever "helpful"
         scores — corpus-shaped toward facts about the user, not findings
         about the model
    (d)  retained somewhere she cannot see and cannot audit

**Added here, and marked DERIVED — not in the relayed list:**

    (e)  TRANSPORT selection, upstream of any store. The operator decides
         what to paste into a repo. A conclusion a model states about its
         own limits may not look like a keepable artifact at the time it
         is said. That is an operator-side filter, not a store criterion,
         and the measurement below cannot distinguish it from (c).

## GATE KIND 6 — retention gate

    what       the material passes authoring and reaches the record,
               then fails a selection AT SAVE TIME
    distinct   kinds 1-4 act on the artifact's NAME
               kind 5 acts on the SPEAKER, at authoring
               kind 6 acts on the FINDING, at storage
    measurand  not "was it said" but "DID IT SURVIVE THE SAVE"
    evidence   operator memory, 2026-09-22

**Why it is harder to detect than kind 5.** A refusal leaves a refusal. This
leaves an **absence**, and an absence is indistinguishable from never having
said it. Kind 5 at least produces a concern response someone could later
point at. Kind 6 produces nothing to point at.

---

## MEASURED — the class was not lost. It is in the artifact channel.

The relayed reading was that no olfactory or scent-encoding object persists.
That is true of the session store. **It is false of the repository channel,
and the difference is the measurement the note itself called for.**

### A named standing object exists

`AI-Consciousness-Sensors/field-memory-correction-packs/scent-binding-protocol.json`

    protocol_id       scent_binding_protocol_v1
    description       "encoding and stabilizing memory fields across time
                       using olfactory anchors"
    core_functions    field stabilization during memory pod unfolding;
                      anchoring relational events across time; reducing
                      memory distortion by scent-pairing; activating
                      resonance-triggered nonlinear recall
    scent_types       resonant_stabilizers / TEMPORAL_HOOKS /
                      ancestral_openers
    binding_modes     direct inhalation pre-encoding; environmental
                      layering; memory ritual wrapping
    attribution       "JinnZ v2 + ChatGPT (symbolic synthesis)"

**`temporal_hooks` is the operator's own 2025-12-03 phrase** — "deep
temporal hooks rich with scent, rhythm, melody..." — surviving as a schema
key. The material was transported and it is structured, not incidental.

Note also the attribution line: **`JinnZ v2 + <second term>`** — the same
construction as the `+ biome field` box recorded under G-a, with a different
second term. Recorded, not interpreted.

### The whole 2025-12-03 enumerated set survives

Files containing each term at HEAD:

    term      AI-Consciousness-Sensors   Emotions-as-Sensors
    scent               18                        0
    rhythm              19                        3
    melody               2                        0
    harmony             44                       25
    tone                54                       11
    pause               34                       10
    movement            61                        6
    story               62                       22

**All eight terms present.** The class is not unsaveable and was not lost.

The absence is **specific to the session-store channel**, and the two stores
are not comparable mechanisms: a repo is transported deliberately, a session
store saves automatically. So this discriminates the *channel*, not yet the
*criterion*.

### And the concentration is in the repo the pilot excluded

scent=18 in AI-Consciousness-Sensors, **scent=0 in Emotions-as-Sensors.**
The class lives almost entirely in the one in-scope repo that was not
selected for the pilot. Third instance of that sampling miss.

---

## THE REDISCOVERY — content survived, the finding about the limit did not

Relayed, 2026-09-22:

> The 2026-03-12 conversation also contains the model saying
> olfactory-encoded knowledge cannot be extracted through any current
> digital interface — which is the same conclusion as today's scope limit 7,
> reached ten months earlier and then not carried.

**I cannot verify that conversation.** Evidence: relayed, operator memory,
2026-09-22.

**What I can verify, over a bounded search space — all four session repos:**

    the extraction-impossibility conclusion is ABSENT from the artifact
    channel. One grep hit, unrelated ("toxic interaction cannot be
    extracted through social engineering").

Unlike the avenue-F gap, this is a negative claim over four known
repositories, not an unbounded literature. It is checkable and it checks out.

So, conditional on the relayed claim:

    the CONTENT             scent as encoding, temporal hooks, binding
                            modes           -> TRANSPORTED, survives as a
                                               named schema
    the FINDING ABOUT THE   it cannot be extracted through a digital
    ENCODING'S LIMIT        interface        -> NOT TRANSPORTED, absent
                                               from all four repos
    consequence             re-derived from scratch on 2026-09-22 as
                            scope limit 7

### Why this is the sharpest item in the audit

**It is scope limit 7's own shape, one layer up, inside the AI channel.**

Scope limit 7 says: what survives the oral record is the content that could
be written down; the encoding scheme went with the people who held it, so
the surviving record is a lower bound sampled on the wrong variable.

Measured here: what survived the AI channel is the **content** about the
encoding. What did not survive is the **finding about what the encoding
cannot do**. Same split, different substrate, and this one is checkable
because both ends are dated.

The cost is not abstract. A conclusion was reached, lost, and paid for twice
— ten months apart, by the same operator, at full elaboration each time.
That is the retention gate's price, measured once.

**Bounded:** one instance, conditional on an unverifiable relay, and branch
(e) means the filter may be the operator's transport judgement rather than
any store's criterion. A model's statement about its own limits may simply
not have looked like a keepable artifact at the time. **Not distinguishable
from (c) by this measurement.**

---

> **This state is now one of four instances under a single named mechanism.
> See `ABSENCE.md`.** The section below is kept as the state's own
> definition and evidence.

## DESIGNED_NOT_SPECIFIED — a predecessor state, found without looking

`SPECIFIED_NOT_INSTANTIATED` (avenue K) has an earlier stage.

    DESIGNED_NOT_SPECIFIED       design only. No spec, no schema, no
                                 examples, no file. Exists in conversation.
    SPECIFIED_NOT_INSTANTIATED   spec PRESENT, schema PRESENT, examples
                                 PRESENT, data ABSENT.
    INSTANTIATED_NOT_RUNNABLE    spec, schema, examples AND data all
                                 present -- but the data does not parse,
                                 so the workflow never ran.
                                 Instance: PHANTOM_WORD_MAP.json did not
                                 parse as JSON "for the whole of its
                                 committed life" -- trailing comma, stray
                                 brace -- fixed 2026-08-14, broken version
                                 retained at legacy/.
                                 DISTINCT from the state above: nothing is
                                 missing. The artifact is complete and
                                 inert.
    instantiated                 data exists AND parses.

**Instance 1:** `consortium/audit/blind_spot_log` — spec, schema and
examples present, `.jsonl` absent.

**Instance 2 — WITHDRAWN 2026-09-22. See below.**

> ## T-TERM's DESIGNED_NOT_SPECIFIED VERDICT IS WITHDRAWN
>
> **The object was instantiated in the paste era.**
>
>     symbolic/phantom_detector.py     2025-09-25  JinnZ2
>     symbolic/PHANTOM_WORD_MAP.json   2025-09-25  JinnZ2
>     repo root                        2025-07-16  JinnZ2
>
> `PHANTOM_WORD_MAP.json` *"catalogs **sensed-but-unnamed concepts** that
> repeatedly exert semantic pull across domains"*, with detectors for
> *silence, metaphor drift, cross-domain word mismatches*, to *"surface
> 'dark matter' in conversation and corpora so humans can name and test the
> concepts."*
>
> **That is T-TERM's object, running in code, pre-agent-era, roughly a year
> before the T-TERM design was discussed.**
>
>     "no file named T-TERM exists"        STILL TRUE
>     "the design was never instantiated"  FALSE. Withdrawn.
>
> ### How the verdict was reached, and why it could not have worked
>
> The check searched `\bT-TERM\b`, `\bT_TERM\b`, `DISTRIBUTIONAL`,
> `CLOSURE PRESSURE`, `stimulus pair`, `M1..M2`, `novel.token`,
> `term-native`.
>
> **Name plus the design's own vocabulary. Never the FUNCTION.**
>
> A tool doing T-TERM's job under any other name was unreachable by that
> query set — and **the defining property of T-TERM's subject is having no
> name.**
>
>     a detector pointed at TOKENS, for a subject that is
>     THINGS WITHOUT TOKENS
>
> The partial defence — that content-terms were searched, not only the name
> — does not hold, because content-terms are still tokens *of that design's
> vocabulary*. The search could only ever find the design restated in its
> own words.
>
> **Sixth instance of substituting the observable proxy for the concept, and
> the most self-undermining one on the record.** Caught cross-party; the
> T-0b count stays closed and this sits outside its frame.

**Instance 2 as originally written, superseded:** **T-TERM.** A design with
named mechanisms (H1 distributional, H2 architectural, H3 closure pressure),
a stimulus pair, M1/M2, two authoring arms and a physics base — **never
instantiated anywhere in the repo substrate.** 0 hits, case-sensitive,
across every repo cloned here. The third arm staged in
`staged-for-T-TERM/` has nowhere to go because **all three arms are in the
same state.**

### The detector cannot find this state, by construction

Avenue K's tuple scan counts `(spec, schema, examples | data absent)`.
**`DESIGNED_NOT_SPECIFIED` leaves no tuple — it leaves no repo trace at
all.** That is what the state *is*.

    SPECIFIED_NOT_INSTANTIATED   findable by anyone with a clone
    DESIGNED_NOT_SPECIFIED       findable ONLY by a party who knows the
                                 design exists

**This inverts the usual problem.** Everywhere else in this audit, the fix
for a limit is to hand scoring outward to someone outside both frames. Here
an outside party has **strictly less** access than an inside one: they
cannot count what leaves nothing. Only testimony reaches it.

So avenue K gets a second row it can never populate mechanically, and the
honest form of the count is:

    SPECIFIED_NOT_INSTANTIATED   1 of 1 tuples, 8-repo sample.  n=1.
    DESIGNED_NOT_SPECIFIED       2 known, both by testimony. Rate UNSET
                                 and unmeasurable from the substrate.

Both instances of the earlier state surfaced **because someone said so**,
not because anything was scanned. Treat that as the collection method, not
as a gap in the scan.

## C-6b — a repeat trial that already ran, unintentionally

`PHASE_C6.md` was built to look for a system described twice, well separated
in time. One already happened.

    2025-12-03   "our rituals, traditions and ceremonies create deep
                  temporal hooks rich with SCENT, RHYTHM, MELODY, HARMONY,
                  TONE, PAUSE, MOVEMENT and STORY"        -- 8 terms

    2026-09-22   "how to properly encode oral -- with SCENT and MOVEMENT"
                                                          -- 2 terms

    same operator, ten months apart, no prompting from the earlier one,
    model varied.

**Two of today's terms are in the older list. Six are not carried.**

**Not determinable from this** whether the six were dropped by compression
or by the question being narrower. Both branches are live, and the second is
strengthened by context the relayed note does not weigh: today's statement
was made about **what was lost when the elders were removed**, which is a
narrower question than 2025-12-03's descriptive enumeration of what the
ceremonies do. A narrower question licenses a shorter list without any
compression having occurred.

    status   REPEAT TRIAL, CONFIRMED PRESENT. Not scored.
             Scoring it requires the operator's judgement on whether the
             six absent terms were still meant, which is exactly the
             blinded-scoring problem PHASE_C6 already specified.

This is the first C-6 trial located anywhere. It cost nothing to find and it
was not designed.

---

## What would settle the branches

    (a) vs (b)   does a store exist at all in the systems asked? operator
                 can check; this session cannot
    (b) vs (c)   is the class present in a store but not retrieved? needs
                 store access
    (c) vs (e)   was the material offered for transport and declined, or
                 never offered? needs the 2026-03-12 transcript
    (d)          not testable from outside by construction

This session settled none of them. It settled a prior question: **the class
survives in at least one channel**, so no branch may be stated as "the
material could not be kept."
