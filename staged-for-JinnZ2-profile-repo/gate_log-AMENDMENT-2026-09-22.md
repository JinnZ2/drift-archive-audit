# To append to gate_log.md, under the header

## AMENDMENT 2026-09-22 — MEASURAND CORRECTION

This ledger was built assuming §2 cases: a slug was chosen by the operator,
could have been otherwise, and the gate is logged on that choice.

Operator statement, 2026-09-22: **all repo names, and the concept vocabulary
inside the repos, are the AI's.** Each was whichever handle the model
available at that date understood and could operate in. The operator followed
the model into its frame and described from inside that model's capability.
Mandala, Rosetta, glyph, polyhedral, conceptual, symbolic — model handles,
not operator selections.

**Consequence:** every row below records MODEL VOCABULARY AVAILABILITY at a
date, not an operator naming decision. Rows, slugs and evidence stand
unchanged; only the measurand moves. Same form as the authorship correction —
the artifact is unaltered, the attribution is corrected.

**Secondary use:** the handles are dated. If they cluster by era with the
content held constant, that is model vocabulary drift, readable as a C-6 arm.

### Worked case, operator memory 2026-09-22

    content   under linear programming, attacked linearly, P versus NP is a
              mathematical problem, and that stands. From another
              perspective it is not the problem it appears to be.
    handle    "mandala". The frame that model could hold it in.
              Not the operator's word for it.

### Measured, in the drift-archive-audit pilot

The secondary use was tested across Bio-Grid-American-Manufacturing-,
Keystone-Codex and Emotions-as-Sensors, 25 candidate handles, first
appearance by `git log -S`:

    agent-era-only handles      metrolog, differential-frame, seed-geometry
                                (3 of 25)
    cross-repo synchrony        metrolog — 17 days apart, two unrelated
                                repos, absent before

**Limit found, and it is load-bearing:** git records the IMPORT date, not the
authoring date. Both pre-agent repos published months of accumulated work in
a single bulk commit (Bio-Grid 67 files 2025-07-10; Emotions 172 files
2025-12-15), so every pre-agent handle collapses onto one date per repo.

**The arm is runnable on the agent era and not runnable before it.**
Recovering the pre-agent half needs dated sources outside git — chat exports,
notes, pre-import file mtimes. Those are UNSET.

See `SPEAKER_GATES.md` for gates that acted on the speaker rather than on an
artifact's name.
