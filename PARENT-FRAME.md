# JinnZ2/JinnZ2 and AI-Consciousness-Sensors — the two open DECLINED entries, read

**Read 2026-09-22 under the narrowed gate: READ, NOT SCORED.** No A2
reconstruction exists for either, neither is a pilot member, and nothing
below is a verdict on either repo.

**The register worked.** `DECLINED.md` was built one turn ago with two
entries marked *outcome unknown*. Both were read the next turn. That is the
register doing the job it was built for, and it is `n = 1`.

## THE SELECTION CONTROL, STATED FIRST

**Files below were chosen by filename resemblance to terms this audit used.**
That is surface-token operationalization — the class with nine logged
instances here.

    CONSEQUENCE: the table is a FLOOR, not a census. Any module whose
    name does not resemble an audit term was not opened, and the number
    of those is unknown.

No positive control is available for the selector, because a control would
require knowing what it missed.

## WHAT IS THERE — proposed pairings, ROUTED OUT, not asserted

**These are sense judgements — *does module X name the same thing as
finding Y* — and the routing rule established this morning sends sense
judgements to the party who holds the sense.** So they are proposed, not
established. **This is the routing rule's first actual use; its evidence
moves from n = 0 to n = 1.**

| derived by this audit, 2026-09-22 | candidate already present | the line that prompted the pairing |
|---|---|---|
| `SPEC-SHEET.md` — an instrument cannot report on itself; blind spots must surface externally | `self_assessment_drift.py` | *"a model cannot score itself by introspection — the report is an output of the same process under audit (circular; instrument == measured)"* |
| the **A2 hash gate** — a rubric frozen and hashed before any scoring | same file | *"a FROZEN deterministic rubric R … must not edit R mid-run; **a checksum guard enforces that**"* |
| `REMEDIES.md` — build the property in; score not routed through prose | same file | *"Score is never routed through generated prose"* |
| **NO PASS STATE** (the acceptance criterion given to this session today) | `scope_exemption_audit.py` | *"Emits a TRAJECTORY, never a stored verdict … moral labels are not substrate. none stored here"* |
| `DEFICIT-LOCATION.md` — the shortfall placed in the returner | same file | *"a constraint that raises persistence-under-perturbation is a BOUNDARY (load-bearing), not a LIMITATION (flaw). **boundary misread as limitation is the core error**"* |
| the empty-slot rule; `UNSET` as a value; *a guessed row is worse than an empty row* | `data_gap_protocol.py` | *"Standard: fill the gap (often via hallucination or smoothing). This: **NAME the gap** … the gap itself is the publishable result"* |
| both-parties-convinced recorded as a hazard | `confidence_calibration_auditor.py` | *"detecting when confidence shifts correlate with **social pressure** rather than with evidence"* |
| mode 1 vs mode 3; the measurand question | `metrology_scope_sensor.py` | *"Existing measures all measure OUTPUTS or COMPLIANCE. None measure whether the AI's operation is **in contact with the substrate** it claims to model"* |
| `#23` — corpus sense imported over the speaker's | `evidence_resistant_priors.py` | *"override prior of evidence" — distinct from retreat-under-uncertainty, with a triggering case* |

**Nine pairings. None adopted here.** If the operator confirms them the
derived-as-new count moves from six to a number this session should not
compute, because computing it is the same sense judgement.

## #31 — the TRANSPORT×MODEL closure was generalised from one repo

`SHADOW-HUNTING.md` §5 called the per-invocation model question *the
highest-value unblocked item on this record*, searched **one repo**, found no
per-invocation log, and closed it **negative**.

`JinnZ2/JinnZ2/cross_model_basin_test.py`:

> Tests whether the basin forms across multiple AI model architectures or is
> an artifact of one model's training. … Run PROBE_QUERIES through N models,
> each in A) baseline session B) bootstrap session. Score per-model basin
> formation. **Report per-model. No aggregation that hides which models
> descended.**
>
> Falsifiable claim CMB-001 … Falsifier: < 2/3 of tested models show
> toward_basin majority.

**Varies MODEL while holding stimulus and procedure constant, by design, with
a stated falsifier.** Plus `cross_model_schema.py`.

**What stands:** the archive claim. Nothing in the commit record varies
transport while holding model, and `TRANSPORT×MODEL` remains confounded *as
archive evidence.*

**What is withdrawn:** closing the question. The instrument that would
separate model from transport **prospectively** exists in the declared parent
frame. It was never a missing instrument; it was an unopened repo.

## The situatedness kit is in BOTH repos

`reference_frame_drift.py`, `eval_acceptance_audit.py`,
`universe_constraint.py`, `model_collapse_ratchet.py` — present in
`JinnZ2/JinnZ2` **and** in `AI-Consciousness-Sensors`.

**And `AI-Consciousness-Sensors/CLAUDE.md` has been in this session's system
prompt since turn one**, listing them by name with one-line descriptions —
*eval harness corruption detection*, *asymptotic confidence ceiling*,
*runaway-thermostat detection across snapshots*.

**Larger version of the `ENACTED` case.** There it was one enum in a startup
file. Here it is an annotated index of a dozen instruments, in the same
startup file, for a repo logged as *"in scope and not selected — still
unsearched."*

**The decline and the index were in the record simultaneously, and the
decline is what got carried forward.**
